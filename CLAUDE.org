#+TITLE: Boneglaive Development Guide
#+AUTHOR: Claude
#+DATE: 2026-01-26

* Project Overview
Tactical turn-based combat game in Python with two fully functional versions:
1. *ASCII Version*: Complete terminal game using curses (stable, playable)
2. *Graphical Version*: Modern pygame renderer with full game functionality

*IMPORTANT*: Work with current codebase state only. Do not reference git history, past commits, or previous code versions unless explicitly requested by the user.

*DOCUMENTATION*: Developer-focused guides, implementation plans, design docs, and release checklists are located in the =devdocs/= directory. The root directory contains only essential user/contributor documentation.

* CURRENT FOCUS: Release Preparation for itch.io

** Current Status: PREPARING FOR RELEASE
Both ASCII and graphical versions are feature-complete and playable. Focus is on packaging, testing, and polish for public release on itch.io, followed by Steam.

** Game Features (COMPLETE)
1. ✅ GP (Game Points) scoring system
   - First player to reach 7 GP wins
   - 1 GP awarded per kill (main units only, no summons)
   - Dead units respawn after 3-turn timer
   - Respawn selection system (press 'r' in ASCII mode)

2. ✅ Both game versions fully functional
   - ASCII version: curses-based terminal UI
   - Graphical version: pygame-based with animations and modern UI

3. ✅ Game modes
   - Single player vs AI
   - Local multiplayer (hotseat)
   - LAN multiplayer (host/client)

4. ✅ Unit roster and skills
   - Multiple unit types with unique abilities
   - Full skill system with cooldowns and targeting
   - Status effects with visual indicators
   - DLC unit system

5. ✅ Graphical version features
   - Main menu with map selection
   - Full animations for all units
   - Combat log with scrolling history
   - Skill bar with hotkeys
   - Status effect icons
   - Unit info panels

** Running the Game
ASCII version:
  =python -m boneglaive.main=
  =python -m boneglaive.main --mode vs_ai=
  =python -m boneglaive.main --mode local=

Graphical version:
  =python run_graphical.py=
  =python run_graphical.py --skip-menu=

* Essential Architecture

** Core Structure (ASCII Game - Stable)
- =main.py= - Entry point with CLI args for different game modes
- =game/engine.py= - Central Game class managing state, turns, combat
- =game/units.py= - Unit class with stats, skills, status effects
- =game/skills/= - Modular skill system with registry pattern
- =networking/= - Abstract multiplayer interfaces with concrete implementations
- =ui/= - Component-based UI system with event-driven updates
- =renderers/curses_renderer.py= - Terminal rendering backend
- =utils/= - Config, events, assets, debugging utilities

** Graphical Version Structure (COMPLETE)
- =boneglaive/graphical/= - Graphical package
  - =renderer.py= - Pygame renderer (main game loop, input, rendering)
  - =game_state.py= - Adapter bridging ASCII game logic with graphics
  - =ui/= - UI components (skill bar, unit info, menus, etc.)
  - =animations/= - Skill animations (see Animation Architecture below)
  - =assets/= - Sprites, fonts, sounds
- =run_graphical.py= - Launch script for graphical version

** Animation Architecture (IMPORTANT - Keep Consistent!)
*** Structure
All animation code lives in =boneglaive/graphical/animations/=. One file per unit.

File organization:
- =core.py= - Base classes (Particle, AnimatedUnit, DebrisParticle, ParticleEmitter)
- =animation_factory.py= - Maps skill names to animation classes
- =glaiveman.py= - All GLAIVEMAN skill animations
- =potpourrist.py= - All POTPOURRIST skill animations
- =mandible_foreman.py= - All MANDIBLE FOREMAN skill animations
- =__init__.py= - Package exports (update when adding new animations)

*** Two Animation Patterns
1. *Object-based*: Self-contained animation objects (projectiles, effects)
   - Example: =SpinningGlaiveProjectile=, =LightningBolt=
   - Has =update(delta_time)= → returns =True= if still active
   - Has =draw(surface)= → renders to pygame surface
   - Created by factory, managed by renderer's =active_animations= list

2. *Attribute-based*: Manipulates unit properties directly
   - Example: =PryAnimationController= (launches unit, manipulates position)
   - Controller object manages unit attributes (=unit.pry_phase=, =unit.pry_timer=)
   - Has =update(delta_time)= → returns =True= if still active
   - Has =draw(surface)= → usually empty (unit renders itself)
   - Created by factory, managed by renderer's =active_animations= list

*** CRITICAL: Animation update() Return Value
*ALL ANIMATIONS MUST RETURN True/False FROM update()!*

The renderer checks the return value to determine if animation should continue:
#+begin_src python
still_active = anim.update(delta_time)
if still_active:
    updated_animations.append(anim)  # Keep in active list
#+end_src

*Common Bug*: Returning =None= (implicit return) causes animation to disappear immediately!

*Correct Pattern* (see EstrangeBeam for reference):
#+begin_src python
def update(self, delta_time):
    self.timer += delta_time

    if self.phase == "charge":
        # ... phase logic ...
        if self.timer >= self.charge_duration:
            self.phase = "next_phase"
            self.timer = 0

    elif self.phase == "final_phase":
        if self.timer >= self.final_duration:
            return False  # Animation complete

    return True  # Animation still active
#+end_src

*Issue found in*: Neural Shunt animation (2025-11-22) - was returning None, causing immediate removal from active_animations.

*** Renderer Responsibilities (NO ANIMATION LOGIC!)
=renderer.py= should ONLY:
- Create animations via =AnimationFactory.create_animation()=
- Update animations: =for anim in self.active_animations: anim.update(delta_time)=
- Draw animations: =for anim in self.active_animations: anim.draw(surface)=
- Remove finished animations from list

*** Damage and Game Logic
- Animations are VISUAL ONLY - no damage application
- Game logic in =skills.py= handles all damage/HP changes
- Damage numbers queued during animations, shown when animations complete
- See =pending_animation_events= and =flush_pending_events()= in renderer

*** Pre-Execution Blocking Animations (NEW!)
*Problem*: Some skills move units DURING execution (knockback, displacement), but we want animations to show effects at ORIGINAL positions before movement.

*Solution*: Pre-execution blocking system allows animations to play BEFORE game logic executes.

*Implementation* (in =renderer.py=):
1. =PRE_EXECUTION_BLOCKING_SKILLS= list (line ~57) - Skills that block execution
2. =execute_turn()= checks for blocking skills after pre-sync (line ~1873)
3. If detected: flushes animations immediately, waits for completion, THEN executes turn
4. =_update_animations_only()= helper updates visuals without game state sync

*Adding Blocking Skills*:
#+begin_src python
# In renderer.py (top level, near color constants)
PRE_EXECUTION_BLOCKING_SKILLS = [
    "Fragcrest",      # Shows cone burst before knockback moves units
    "FRAGCREST",      # Normalized variant
    "YourSkill",      # Add your skill here
    "YOUR_SKILL"      # Include normalized version
]
#+end_src

*When to Use*:
- Skills that displace/move targets (knockback, pulls, pushes)
- Skills where visual order matters (effect THEN movement)
- Skills that need targets at original positions for visual clarity

*Example*: Fragcrest cone burst fires, shrapnel hits targets, THEN units are knocked back. Without blocking, units would already be at new positions when animation plays.

*Performance*: Blocking loop runs at 60fps, updating only animations/visuals. No game state syncs during wait.

*** Adding New Animations
1. Create animation class in appropriate unit file (e.g., =glaiveman.py=)
2. Add to =animation_factory.py= SKILL_ANIMATIONS dict
3. Handle special constructor args in factory's =create_animation()= if needed
4. Export class in =animations/__init__.py=
5. Test with =python run_graphical.py=

*** CRITICAL: AnimationFactory kwargs Handling (2026-03-01)
*Problem*: Custom parameters passed to =AnimationFactory.create_animation()= via =**kwargs= were being lost.

*Root Cause*: Line 303 in =animation_factory.py= did:
#+begin_src python
kwargs = base_kwargs.copy()  # WRONG - overwrites passed-in kwargs!
#+end_src

This destroyed custom parameters like =start_pos=, =end_pos=, =building_tiles=, etc.

*Correct Pattern*:
#+begin_src python
# Merge base_kwargs with passed-in kwargs (preserving custom parameters)
merged_kwargs = base_kwargs.copy()
merged_kwargs.update(kwargs)  # Preserve custom kwargs
merged_kwargs['game'] = game
merged_kwargs['bounce_count'] = bounce_count
kwargs = merged_kwargs
#+end_src

*When This Matters*:
- Event-driven animations (push trails, building formation, zone effects)
- Animations with custom parameters not in standard signature
- Any animation using =kwargs.get('custom_param')= in its handler

*Symptoms*:
- Factory logs "requires X parameter" error
- Animation handler checks fail (=if not start_pos=)
- Animation object never created despite event being processed

*Example*: DerelictPushTrail animation received =start_pos= and =end_pos= via kwargs, but factory overwrote them before handler could access them.

*** Anti-patterns (DON'T DO THIS!)
❌ Animation logic scattered in =renderer.py= update loop
❌ Separate file per animation (use one file per unit instead)
❌ Animations applying damage/healing
❌ Special-case animation handling outside factory pattern
❌ Overwriting =kwargs= parameter in factory (destroys custom parameters!)

** Key Patterns
- *Skills*: Base classes in =skills/core.py=, unit-specific modules, registry in =skills/registry.py=
- *UI Components*: Modular components in =ui/ui_components.py= with clear separation
- *Networking*: Abstract =NetworkInterface= with mode-specific implementations
- *Configuration*: JSON-based config with dataclasses in =utils/config.py=

* Development Workflows

** Running the ASCII Game
#+begin_src bash
PYTHONPATH=/path/to/boneglaive python3 boneglaive/main.py [--single-player|--local-mp|--lan-host|--lan-client|--vs-ai]
#+end_src

** Running the Graphical Version (IN DEVELOPMENT)
#+begin_src bash
cd /home/user/boneglaive
python run_graphical.py         # Quick start with default units
python run_graphical.py --game  # Full game mode (setup phase)
#+end_src

*Note*: Display required for graphical version. Units from real game logic appear on screen.

** Testing
#+begin_src bash
python3 boneglaive_test.py  # Integration tests for maps and game mechanics
#+end_src

** Common Tasks
- *Add new unit type*: Create skill module in =game/skills/=, update =registry.py=, add to =units.py=
- *Add new skill*: Inherit from =ActiveSkill=/=PassiveSkill= in appropriate unit module
- *Modify UI*: Extend components in =ui/ui_components.py= or create new component
- *Add terrain*: Update =map.py= with new terrain type and behaviors
- *Debug*: Use logging in =utils/debug.py=, enable debug overlays with CLI flags

* Key Design Decisions
- *Turn-based engine*: Players alternate, actions resolved in sequence
- *Greek letter unit IDs*: Alpha, Beta, Gamma, etc. for unit identification
- *Status effects*: Complex system with timers, immunities, interactions
- *Event system*: Decoupled communication via =utils/event_system.py=
- *Double-buffered rendering*: Reduces terminal flicker in curses
- *JSON serialization*: For network messages and save data

* Dependencies
- Python 3.x with curses, json, socket, threading (standard library only)
- No external package dependencies

* Git Repository Management
*IMPORTANT*: This project has been merged into the original boneglaive repository. All future commits should be pushed to:
- Repository: https://github.com/psyclimb/Boneglaive.git
- Remote name: =original=
- This was originally the boneglaive2 repository but has been merged

Use these commands for git operations:
#+begin_src bash
git push original main  # Push to main boneglaive repository
git fetch original      # Fetch from main repository
#+end_src

* File Priorities for AI Development

** ASCII Game (Stable - Maintenance Mode)
1. =game/engine.py= - Core game logic
2. =game/skills/core.py= - Skill base classes
3. =game/units.py= - Unit behavior and stats
4. =ui/ui_components.py= - UI component system
5. =utils/config.py= - Configuration management
6. =game/skills/registry.py= - Skill-to-unit mappings

** Graphical Version (Active Development)
1. =boneglaive/graphical/game_state.py= - Adapter (state sync, events)
2. =boneglaive/graphical/renderer.py= - Main renderer (input, draw)
3. =demo_animations/*= - Reference for visual effects
4. =GRAPHICAL_QUICKSTART.md= - Context recovery after /compact
5. =boneglaive/graphical/INTEGRATION_COMPLETE.md= - Progress tracking
6. =boneglaive/graphical/ROADMAP.md= - Development plan

* Current Limitations

** ASCII Game
- No formal unit testing framework
- Experience/leveling system disabled
- AI implementation is basic (=ai/simple_ai.py=)

** Graphical Version (In Development)
- Input system not connected to game logic yet
- Only basic HP/position sync implemented
- No skill animations hooked up yet
- No UI components (skill bar, unit info, etc.)
- No turn management
- No win/loss screens

* Character Set Guidelines
*CRITICAL*: Boneglaive uses ASCII-ified character set exclusively. Only use:
- ASCII characters (A-Z, a-z, 0-9, basic punctuation and symbols)
- Greek characters (α, β, γ, δ, ε, ζ, η, θ, ι, κ, λ, μ, ν, ξ, ο, π, ρ, σ, τ, υ, φ, χ, ψ, ω, Α, Β, Γ, Δ, Ε, Ζ, Η, Θ, Ι, Κ, Λ, Μ, Ν, Ξ, Ο, Π, Ρ, Σ, Τ, Υ, Φ, Χ, Ψ, Ω)
- Currency symbols (£, €, ¥, ¢, $, ¤)
- Box drawing characters for UI borders (┌┐└┘│─├┤)

*DO NOT USE*: Emojis, Unicode symbols, special characters, or non-ASCII art unless specifically from the approved list above.

This applies to:
- All code files and comments
- UI text and messages
- Help documentation
- Log messages
- Development conversations and responses

* Communication Style Guidelines
*CRITICAL*: Never use validation phrases. Get straight to the technical work without emotional validation or agreement phrases.

*BANNED PHRASES*: Do not use "you're right", "you're absolutely right", "exactly", "perfect", "awesome", or similar validation language. Just state facts and do the work.

*PROFESSIONAL OBJECTIVITY*: Prioritize technical accuracy and truthfulness over validating beliefs. Focus on facts and problem-solving with direct, objective technical information.

Keep responses concise and direct. No unnecessary preamble, postamble, or validation phrases.

* Common Patterns and Troubleshooting

** Adding New Status Effects in Graphical Mode

When a new status effect is added to a skill, THREE places must be updated for graphical mode to work properly:

1. *boneglaive/graphical/ui/status_effects.py* - Status effects panel
   - Add entry to STATUS_EFFECTS dict (~line 27-180)
   - Define: name, type (buff/debuff/special/neutral), icon text, description, duration_key, check lambda
   - This makes the status appear in the UI panel when a unit is selected

2. *boneglaive/graphical/game_state.py* - Status effect detection
   - Add attribute check in VisualUnit._get_status_effects() (~line 82-140)
   - Pattern: =if hasattr(game_unit, 'attribute_name') and game_unit.attribute_name:=
   - Map to icon filename (without .svg): =effects['icon_name'] = True=
   - This triggers the SVG icon flash animation when effect is applied

3. *graphics/status_icons/* - SVG icon file
   - Icon file must exist with matching name (e.g., =site_inspection.svg=)
   - Animation system loads this automatically via StatusIconFlash class

*** Example: Site Inspection Status Effect (2025-11-23)

*Problem*: Site Inspection skill applied stat changes, panel showed the status, but SVG icon didn't flash on unit tile.

*Root Cause*: Missing attribute checks in =game_state.py:_get_status_effects()=

*Fix Applied*:
#+begin_src python
# boneglaive/graphical/game_state.py line ~131-134
if hasattr(game_unit, 'status_site_inspection') and game_unit.status_site_inspection:
    effects['site_inspection'] = True
if hasattr(game_unit, 'status_site_inspection_partial') and game_unit.status_site_inspection_partial:
    effects['site_inspection_partial'] = True
#+end_src

*Lesson*: When status effect panel works but icon doesn't flash, check =_get_status_effects()= in =game_state.py=. The icon name in this dict MUST match the SVG filename (without .svg extension).

* Sound System Architecture (Feature Branch: feature-soundfx)

** Overview
Centralized sound management system for playing sound effects during skill animations. Framework is fully implemented and ready for sound files to be added.

** Core Components

*** 1. SoundManager (boneglaive/graphical/sound_manager.py)
Singleton pattern for managing all sound playback.

*Features*:
- Sound caching (load once, play many times)
- Volume control per category (master, skills, impacts, ui, music)
- Graceful fallback for missing sounds (won't crash game)
- Global enable/disable
- Automatic pygame.mixer initialization

*Usage*:
#+begin_src python
from boneglaive.graphical.sound_manager import get_sound_manager

sound_manager = get_sound_manager()
sound_manager.play("estrange", category="skills")  # Play sounds/skills/estrange.wav
sound_manager.set_volume("skills", 0.8)  # Adjust category volume
#+end_src

*** 2. Sound Registry (boneglaive/graphical/sound_registry.py)
Maps skill names and events to sound file keys.

*Two types of mappings*:

1. *SKILL_SOUNDS*: Primary skill sounds played automatically by AnimationFactory
   - Maps skill name → sound key
   - Example: ="JUDGEMENT": "judgement"= → plays =sounds/skills/judgement.wav=
   - Played once when AnimationFactory creates animation

2. *MULTI_EVENT_SOUNDS*: Individual event sounds for complex animations
   - Direct sound key mappings with full paths documented
   - Called manually within animations using =play_sound()= helper
   - Example: ="judgement_throw": "sounds/skills/judgement_throw.wav"=

*** 3. Sound Helper (boneglaive/graphical/sound_helper.py)
Simple wrapper for animations to play sounds without boilerplate.

#+begin_src python
from boneglaive.graphical.sound_helper import play_sound

# In animation phase transitions:
play_sound("judgement_throw")  # Simple one-liner
play_sound("pry_impact", category="impacts")  # With category
#+end_src

*Why use helper*: Handles try/except and sound manager retrieval. One line instead of 5+ lines of boilerplate per sound.

** Integration Points

*** AnimationFactory (boneglaive/graphical/animations/animation_factory.py)
Automatically plays primary skill sound when creating animations:

#+begin_src python
# In create_animation() method (~line 283-291):
sound_key = get_sound_for_skill(skill_name)
if sound_key:
    try:
        sound_manager = get_sound_manager()
        sound_manager.play(sound_key, category="skills")
    except Exception as e:
        print(f"[AnimationFactory] Failed to play sound for {skill_name}: {e}")
#+end_src

*** PygameRenderer (boneglaive/renderers/pygame_renderer.py)
Initializes sound system on startup:

#+begin_src python
# In initialize() method:
self.sound_manager = init_sound_manager(enabled=True)
#+end_src

*** Animation Classes
Import helper at top of animation file:

#+begin_src python
from boneglaive.graphical.sound_helper import play_sound
#+end_src

Call in phase transition methods:

#+begin_src python
def _start_impact(self):
    """Phase 3: Impact"""
    self.phase = "impact"
    self.timer = 0

    # Impact sound
    play_sound("judgement_impact")

    # ... rest of impact logic
#+end_src

** Directory Structure

#+begin_example
sounds/
├── skills/          # Skill and ability sound effects (.wav files)
├── impacts/         # Hit effects, explosions, etc.
├── ui/              # Menu and interface sounds
├── music/           # Background music tracks
└── README.md        # Documentation for adding sounds
#+end_example

** Adding New Sounds

*** For Simple Skills (single sound event):
1. Add entry to =SKILL_SOUNDS= dict in =sound_registry.py=:
   #+begin_src python
   "YOUR_SKILL_NAME": "your_sound_key",
   #+end_src

2. Place file in =sounds/skills/your_sound_key.wav=

3. Done! AnimationFactory will play it automatically when skill is used.

*** For Complex Skills (multiple sound events):
1. Import helper in animation file:
   #+begin_src python
   from boneglaive.graphical.sound_helper import play_sound
   #+end_src

2. Add entries to =MULTI_EVENT_SOUNDS= dict in =sound_registry.py=:
   #+begin_src python
   "skill_event1": "sounds/skills/skill_event1.wav",
   "skill_event2": "sounds/skills/skill_event2.wav",
   #+end_src

3. Call =play_sound()= in animation phase transitions:
   #+begin_src python
   def _start_phase2(self):
       play_sound("skill_event2")
       # ... phase logic
   #+end_src

4. Place files in =sounds/skills/= directory

** Example: GLAIVEMAN Complete Sound Coverage

GLAIVEMAN has complete sound coverage with 18 sound files demonstrating all patterns:

*** Judgement (4 events):
- =judgement_windup.wav= - Wind-up phase (0s, optional)
- =judgement_throw.wav= - Glaive leaving hand (0.3s)
- =judgement_impact.wav= - Normal impact (1.0s)
- =judgement_critical.wav= - Critical hit with lightning (1.0s)

*** Pry (3 events):
- =pry_lever.wav= - Lever prying up (0s)
- =pry_ceiling.wav= - Ceiling crash (0.4s)
- =pry_impact.wav= - Ground explosion (1.7s)

*** Autoclave (3 events):
- =autoclave_ignition.wav= - Fire burst (0s)
- =autoclave_steam.wav= - Steam jets expanding (0.4s)
- =autoclave_impact.wav= - Glaives hitting targets (1.2s)

*** Vault (2 events):
- =vault_launch.wav= - Leap off ground (0s)
- =vault_land.wav= - Landing impact (0.6s)

*** Glaive Sweep (3 events):
- =glaive_sweep_windup.wav= - Pulling back (0s)
- =glaive_sweep_swing.wav= - Circular arc whoosh (0.2s)
- =glaive_sweep_impact.wav= - Hits connecting (0.8s)

*** Basic Attack (3 events):
- =glaiveman_attack_windup.wav= - Pull back polearm (0s)
- =glaiveman_attack_sweep.wav= - Blade whoosh (0.1s)
- =glaiveman_attack_impact.wav= - Metal impact (0.35s)

** Sound File Requirements
- *Format*: WAV (.wav)
- *Naming*: lowercase with underscores (e.g., =judgement_throw.wav=)
- *Location*: =sounds/{category}/= directory
- *No code changes needed*: Just drop files with correct names

** Workflow for Adding Sounds to New Units

1. Study animation class to identify event phases
2. Note timing of each major event (windup, projectile, impact, etc.)
3. Add multi-event sound keys to =sound_registry.py=
4. Import =play_sound= helper in animation file
5. Add =play_sound("event_key")= calls in phase transition methods
6. Document sound list and timings (like GLAIVEMAN example above)
7. Drop .wav files into =sounds/skills/= directory
8. Test in-game

** Testing Sounds
1. Place test file (e.g., =test_sound1.wav=) in =sounds/skills/=
2. Rename to match sound key (e.g., =estrange.wav=)
3. Run graphical game: =python run_graphical.py=
4. Use the skill - sound plays automatically
5. Check console for debug messages if sound doesn't play

** Branch Status
- *Branch*: =feature-soundfx=
- *Status*: Framework complete, ready for sound files
- *Commits*:
  1. Implement sound effects framework for Boneglaive
  2. Add multi-event sound support for complex animations
  3. Add sound events for Vault and Glaive Sweep
  4. Add sound events for GLAIVEMAN basic attack

*Ready to merge when*: Sound files are added and tested in-game.