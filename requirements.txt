# Boneglaive Dependencies

# Core game dependencies (cross-platform)
# All dependencies are from Python standard library except for platform-specific ones

# Windows-specific dependencies
windows-curses; sys_platform == "win32"

# Optional dependencies for enhanced features
# pygame is optional but recommended for graphical rendering
pygame; python_version >= "3.8"

# Development/testing dependencies (optional)
# These are not required for basic gameplay but useful for development
# pytest; extra == "dev"
# pytest-cov; extra == "dev"