# Boneglaive Dependencies
# Linux and BSD Terminal Gaming

# Core game dependencies
# All core dependencies are from Python standard library (curses, json, socket, threading)

# Optional dependencies for development
# pygame kept for potential future enhancements (not user-facing)
# pygame; python_version >= "3.8"

# Development/testing dependencies (optional)
# These are not required for basic gameplay but useful for development
distro>=1.5.0  # For better Linux distribution detection
# pytest; extra == "dev"
# pytest-cov; extra == "dev"